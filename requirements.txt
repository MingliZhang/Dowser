fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7

# Network capture — the primary detection path.
playwright>=1.45

# Fallback extractor for pages where capture comes up empty.
yt-dlp>=2025.1.1
